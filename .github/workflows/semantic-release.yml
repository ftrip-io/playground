name: CI
on: push
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-node@v1
        with:
          node-version: '18'
      - name: Release
        env:
          GITHUB_TOKEN: ${{ secrets.MP_SECRET }}
        run: npx semantic-release